import machine
import urequests
import json

# Netzwerkschnittstelle einrichten
network = network.WLAN()
network.connect('SSID', 'PASSWORT')

# API-Endpunkt und API-Schlüssel
url = 'http://api.openweathermap.org/data/2.5/weather?q=Stadt&APPID=DEIN_API_SCHLUESSEL'

# HTTP GET Request senden
response = urequests.get(url)
data = json.loads(response.content.decode())
print(data)